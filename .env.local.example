# Copy to .env.local and set OPENAI_API_KEY to enable the model engine.
# .env.local is gitignored — your key never gets committed.

OPENAI_API_KEY=

# Optional: override the model (any OpenAI chat model id).
# TWIN_MODEL=gpt-4o-mini
